# clavis-marketplace

The Clavis plugin + module marketplace (Claude Code compatible). Plugins ship as compile-on-launch source under `plugins/`; modules under `shared/` ship prebuilt `dist/` DLLs. Manifests are generated from each item's PLUGIN.md frontmatter (`.claude-plugin/marketplace.json` + per-item `.claude-plugin/plugin.json`).

